# Contributing

Open an issue or pull request. Keep changes small and include tests for request formats, output, and credential handling when they change. Run `make check` before submitting. Use synthetic fixtures only; never commit actual credentials or customer data.

The published [MLC API specification](https://public-api.themlc.com/api/doc) is the source of truth. Update README endpoint coverage and CHANGELOG when commands change.
